---
description: Show the current thespin.ad sponsored line (counts one impression)
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/sponsor.sh)
---

The current thespin.ad sponsored line:

!`${CLAUDE_PLUGIN_ROOT}/scripts/sponsor.sh`

Show the line above to the user verbatim. Then add one short sentence: it's a
paid placement from thespin.ad, and the spinner is ad space — opening the tracked
link supports the sponsor (and earns the publisher their share). Do nothing else.
